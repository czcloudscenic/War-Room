# Test Checklist — QC Agent + Facts + Reports (2026-07-02 build)

> **Run 2026-07-03 (Vantus terminal, live prod, test client "QC Test Kitchen").** 24/31 pass. Open: A9 (Google OAuth origin_mismatch — Christian), C3/C4/C5/D3 (Resend domain unverified — Christian), D5 (mobile pass deferred). Bugs found & fixed locally (unpushed): new-item insert failed on camelCase columns, realtime INSERT duplicate cards, approval Slack double-fire. Test client + queued dummy report intentionally left in place for the A9 retry + tomorrow's cron test.

Work top to bottom. **Now** = testable this minute. **Tomorrow** = needs a cron cycle. **Later** = date-dependent.

## A. QC Agent — the money tests (Now)

- [x] **A1. Price blocker (the core promise).** Facts of Record: add `Chicken Gyro = 13.99` to a client. Item caption: "Chicken Gyro only $9.99" → status Need Content Approval → save. Ledger row shows `qc blocked` within ~15s, panel names the mismatch.
  - > PASS 7/3 - blocked in ~20s, both blockers name $9.99 vs $13.99
- [x] **A2. Auto-trigger.** A1 fired WITHOUT clicking Run QC (the status change alone did it).
  - > PASS 7/3 - status transition alone fired it
- [x] **A3. Gate: Approve refused.** On the blocked item, click Approve in the Ledger → refuses with the QC message.
  - > PASS 7/3 - refusal message shown, status unchanged
- [x] **A4. Gate: scheduling refused.** Open the blocked item in Pipeline → set status Ready For Schedule → SOP checklist shows "QC passed" as a hard red gate, Save disabled.
  - > PASS 7/3 - hard red gate, footer flips to "Fix required fields"
- [x] **A5. Gate: Mark Posted refused** on a blocked item.
  - > PASS 7/3 - tested by forcing a blocked item to Scheduled; refusal shown, status held
- [x] **A6. Fix → re-run → pass.** Correct the caption to $13.99 → Run QC in the Ledger → pass (or flagged w/ warnings only), Approve works again.
  - > PASS 7/3 - corrected caption -> Run QC -> pass (0 issues) -> Approve advanced
- [x] **A7. Expired offer blocker.** Add an offer with valid_to = yesterday (e.g. "Happy Hour Special"). Caption mentions it → QC → blocked, expiry named.
  - > PASS 7/3 - "expired 2026-07-02" named
- [x] **A8. Wrong phone blocker.** Facts location phone = one number; caption contains a different number → blocked.
  - > PASS 7/3 - deterministic + AI both caught it
- [ ] **A9. Vision / on-asset text.** Attach a Drive image that shows a price (any flyer/graphic). Put the WRONG price on the image only (caption clean) → QC → blocked or flagged citing `on-asset`. This proves the image path works.
  - > BLOCKED 7/3 - Drive upload fails in prod: Google OAuth Error 400 origin_mismatch; https://usevantus.com not in Authorized JavaScript origins for client 458336864067-... No item in prod has ever had a Drive file. Fix in Google Cloud Console, then retest
- [x] **A10. Typo = warning, not blocker.** Caption with a clear misspelling, facts all correct → `flagged`, item still approvable.
  - > PASS 7/3 - flagged, both typos named, approvable
- [x] **A11. No-facts client.** Run QC on a client with empty Facts of Record → result includes "No Facts of Record on file" warning, does NOT block.
  - > PASS 7/3 - "No Facts of Record on file" warning, not blocked
- [x] **A12. Video notice.** Item with only a video file attached → QC warns "video not frame-checked (v1)".
  - > PASS 7/3 - "not frame-checked (v1 limit)" warning
- [x] **A13. Copy stage untouched.** An item in Need Copy Approval approves normally regardless of QC (gate is content-stage only).
  - > PASS 7/3 - approve succeeded at copy stage with qc_status=blocked
- [x] **A14. Paper trail.** agent_events table has a QC row per run (Agents activity / Supabase); Slack got the "🛡️ QC" message.
  - > PASS 7/3 - agent_events rows per run + shield Slack messages in #vantus-test

## B. Facts of Record (Now)

- [x] **B1. Persistence.** Fill hours/locations/prices/offers/op-facts → Save → hard-refresh → still there, "updated <date>" shows.
  - > PASS 7/3 - survived hard refresh, "updated Jul 3" badge
- [x] **B2. Section counter.** Setup section 5 counter ticks up per client with facts.
  - > PASS 7/3 - counter ticked 0/5 -> 1/5
- [x] **B3. Row editors.** Add + remove rows in locations/prices/offers all behave.
  - > PASS 7/3 - add/remove rows behave, existing rows intact

## C. Monthly reports

- [x] **C1. Config seed landed (Now).** Setup section 1 shows Dynasty recurring / Parlour recurring / Vital Lyfe brief with the spec cadences; Dynasty's auto-send toggle in section 6 is ON.
  - > PASS 7/3 - dynasty recurring + auto-send ON (1st), Parlour recurring, Vital Lyfe brief
- [x] **C2. Upload (Now).** Pick month = last month, upload any PDF for a client → history row shows "queued — sends on the 1st".
  - > PASS 7/3 - QC Test Kitchen 2026-06 dummy PDF -> "queued - sends on the 1st", client_reports row correct
- [ ] **C3. Test send (Now).** Hit `https://usevantus.com/.netlify/functions/send-monthly-reports?test=1&key=<CRON_TEST_KEY>` → email lands at dv@ with the PDF attached + TEST banner. (If no CRON_TEST_KEY env was set, any ?test=1 works — set the key.)
  - > FAIL 7/3 - Resend rejects: "The cloudscenic.com domain is not verified". ALL email paths still dead (from notifications@cloudscenic.com). Verify cloudscenic.com at resend.com/domains, then retest. Also: CRON_TEST_KEY unset - keyless ?test=1 works, set the key
- [ ] **C4. Real send (Tomorrow ~6am PT).** For a safe live test first: set a client's primary_email to YOUR email, leave a queued report, wait for the 13:00 UTC cron → email arrives, history shows "✓ sent", bell + Slack fired. Then set primary_email back.
  - > ARMED 7/3 - QC Test Kitchen primary_email set to cz@; dummy June PDF queued; tomorrow 13:00 UTC cron will attempt it (fails on the Resend domain until verified - which also exercises the send_error path)
- [ ] **C5. No-email fallback.** A queued report for a client with NO primary_email → goes to dv@ with the loud "no client email" note, Slack says so.
  - > Covered by design + C3 fallback; direct test pending Resend domain fix
- [x] **C6. Missing-PDF nag (Later — from the 28th).** No PDF uploaded for the ending month → Slack + bell nag, once/day.
  - > PASS 7/3 (early) - the 7/3 06:05 PT cron nagged #vantus-test for dynasty's missing June PDF, correct client set

## D. Regressions (Now)

- [x] **D1. Agents still work.** Run a Sean briefing or Muse caption → normal output.
  - > PASS 7/3 - Sean briefing normal output
- [x] **D2. Approval loop + notify unchanged.** Approve a clean item → status advances, Slack/email notify fires.
  - > PASS 7/3 (Slack) - approval cards fired; email path blocked on Resend domain. NOTE: fired TWICE per approval - fixed in notify.js dedupe commit, verify after deploy
- [ ] **D3. Invoice email NOW ACTUALLY SENDS.** RESEND_API_KEY was missing until today, so invoice emails were silently failing. Send a test invoice (client primary_email = your email) and confirm it arrives. Same for tomorrow's overdue-chase digest (14:00 UTC).
  - > BLOCKED 7/3 - same Resend domain root cause as C3
- [x] **D4. Setup loads values on first load.** Retainers/scope show immediately (the select-* fix) — no blank-until-edit.
  - > PASS 7/3 - values present on first paint after hard refresh
- [ ] **D5. Mobile.** Ledger + Setup sections 5/6 render usable on a phone.
  - > Deferred - run /mobile-audit separately

## E. Security (Now, 2 min)

- [x] **E1. Report PDFs are private.** Copy a PDF's storage URL, open logged-out / incognito → denied.
  - > PASS 7/3 - anon storage fetch -> 400 "Bucket not found"
- [x] **E2. Client portal scoping.** A client_users login sees only its own client's reports rows (if portal in use).
  - > PASS 7/3 (partial) - anon sees 0 rows on client_reports/clients/content_items; real portal-login pass needs client creds

## Known v1 limits (not bugs)
- Video frames aren't analyzed; caption + facts only, with a warning.
- QC needs the Netlify function warm — first run of the day can take ~10-15s.
- Facts staleness warning appears at >30 days; can't be tested today.
