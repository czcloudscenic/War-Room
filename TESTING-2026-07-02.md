# Test Checklist — QC Agent + Facts + Reports (2026-07-02 build)

Work top to bottom. **Now** = testable this minute. **Tomorrow** = needs a cron cycle. **Later** = date-dependent.

## A. QC Agent — the money tests (Now)

- [ ] **A1. Price blocker (the core promise).** Facts of Record: add `Chicken Gyro = 13.99` to a client. Item caption: "Chicken Gyro only $9.99" → status Need Content Approval → save. Ledger row shows `qc blocked` within ~15s, panel names the mismatch.
- [ ] **A2. Auto-trigger.** A1 fired WITHOUT clicking Run QC (the status change alone did it).
- [ ] **A3. Gate: Approve refused.** On the blocked item, click Approve in the Ledger → refuses with the QC message.
- [ ] **A4. Gate: scheduling refused.** Open the blocked item in Pipeline → set status Ready For Schedule → SOP checklist shows "QC passed" as a hard red gate, Save disabled.
- [ ] **A5. Gate: Mark Posted refused** on a blocked item.
- [ ] **A6. Fix → re-run → pass.** Correct the caption to $13.99 → Run QC in the Ledger → pass (or flagged w/ warnings only), Approve works again.
- [ ] **A7. Expired offer blocker.** Add an offer with valid_to = yesterday (e.g. "Happy Hour Special"). Caption mentions it → QC → blocked, expiry named.
- [ ] **A8. Wrong phone blocker.** Facts location phone = one number; caption contains a different number → blocked.
- [ ] **A9. Vision / on-asset text.** Attach a Drive image that shows a price (any flyer/graphic). Put the WRONG price on the image only (caption clean) → QC → blocked or flagged citing `on-asset`. This proves the image path works.
- [ ] **A10. Typo = warning, not blocker.** Caption with a clear misspelling, facts all correct → `flagged`, item still approvable.
- [ ] **A11. No-facts client.** Run QC on a client with empty Facts of Record → result includes "No Facts of Record on file" warning, does NOT block.
- [ ] **A12. Video notice.** Item with only a video file attached → QC warns "video not frame-checked (v1)".
- [ ] **A13. Copy stage untouched.** An item in Need Copy Approval approves normally regardless of QC (gate is content-stage only).
- [ ] **A14. Paper trail.** agent_events table has a QC row per run (Agents activity / Supabase); Slack got the "🛡️ QC" message.

## B. Facts of Record (Now)

- [ ] **B1. Persistence.** Fill hours/locations/prices/offers/op-facts → Save → hard-refresh → still there, "updated <date>" shows.
- [ ] **B2. Section counter.** Setup section 5 counter ticks up per client with facts.
- [ ] **B3. Row editors.** Add + remove rows in locations/prices/offers all behave.

## C. Monthly reports

- [ ] **C1. Config seed landed (Now).** Setup section 1 shows Dynasty recurring / Parlour recurring / Vital Lyfe brief with the spec cadences; Dynasty's auto-send toggle in section 6 is ON.
- [ ] **C2. Upload (Now).** Pick month = last month, upload any PDF for a client → history row shows "queued — sends on the 1st".
- [ ] **C3. Test send (Now).** Hit `https://usevantus.com/.netlify/functions/send-monthly-reports?test=1&key=<CRON_TEST_KEY>` → email lands at dv@ with the PDF attached + TEST banner. (If no CRON_TEST_KEY env was set, any ?test=1 works — set the key.)
- [ ] **C4. Real send (Tomorrow ~6am PT).** For a safe live test first: set a client's primary_email to YOUR email, leave a queued report, wait for the 13:00 UTC cron → email arrives, history shows "✓ sent", bell + Slack fired. Then set primary_email back.
- [ ] **C5. No-email fallback.** A queued report for a client with NO primary_email → goes to dv@ with the loud "no client email" note, Slack says so.
- [ ] **C6. Missing-PDF nag (Later — from the 28th).** No PDF uploaded for the ending month → Slack + bell nag, once/day.

## D. Regressions (Now)

- [ ] **D1. Agents still work.** Run a Sean briefing or Muse caption → normal output.
- [ ] **D2. Approval loop + notify unchanged.** Approve a clean item → status advances, Slack/email notify fires.
- [ ] **D3. Invoice email NOW ACTUALLY SENDS.** RESEND_API_KEY was missing until today, so invoice emails were silently failing. Send a test invoice (client primary_email = your email) and confirm it arrives. Same for tomorrow's overdue-chase digest (14:00 UTC).
- [ ] **D4. Setup loads values on first load.** Retainers/scope show immediately (the select-* fix) — no blank-until-edit.
- [ ] **D5. Mobile.** Ledger + Setup sections 5/6 render usable on a phone.

## E. Security (Now, 2 min)

- [ ] **E1. Report PDFs are private.** Copy a PDF's storage URL, open logged-out / incognito → denied.
- [ ] **E2. Client portal scoping.** A client_users login sees only its own client's reports rows (if portal in use).

## Known v1 limits (not bugs)
- Video frames aren't analyzed; caption + facts only, with a warning.
- QC needs the Netlify function warm — first run of the day can take ~10-15s.
- Facts staleness warning appears at >30 days; can't be tested today.
